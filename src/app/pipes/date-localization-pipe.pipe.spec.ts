/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateLocalizationPipePipe } from './date-localization-pipe.pipe';

describe('Pipe: DateLocalizationPipee', () => {
  it('create an instance', () => {
    let pipe = new DateLocalizationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
